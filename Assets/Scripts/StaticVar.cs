using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StaticVar : MonoBehaviour
{
    //save the score as static variable because we need need it in different scences
    public static int scoreSave;
}
